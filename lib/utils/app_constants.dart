enum Gender {
  male,
  female,
  other,
}

enum FoodType {
  veg,
  nonVeg,
}

enum BedRidden {
  yes,
  no,
}

enum VehicleType {
  twoVehicle,
  fourVehicle,
}

enum TimeEnum {
  week,
  halfMonth,
  fullMonth,
}

enum WorkingTime {
  morning,
  afternoon,
  evening,
}
